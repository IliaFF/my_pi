--- a/dist/codex-api.js
+++ b/dist/codex-api.js
@@ -17,12 +17,30 @@
     }
     const payload = parseJsonObject(text, "Codex usage endpoint response");
     const rateLimit = assertObject(payload.rate_limit, "Codex rate limit");
-    const primary = normalizeWindow(rateLimit.primary_window, "5-hour Codex usage window");
-    const secondary = normalizeWindow(rateLimit.secondary_window, "weekly Codex usage window");
-    return {
-        fiveHour: primary,
-        weekly: secondary,
-    };
+    const rawWindows = [rateLimit.primary_window, rateLimit.secondary_window]
+        .filter((value) => value && typeof value === "object" && !Array.isArray(value));
+    if (rawWindows.length === 0) {
+        throw new Error("Codex rate limit did not include an active usage window.");
+    }
+    let fiveHour;
+    let weekly;
+    for (const [index, rawWindow] of rawWindows.entries()) {
+        const seconds = asNumber(rawWindow.limit_window_seconds);
+        const normalized = normalizeWindow(rawWindow, "Codex usage window");
+        if (seconds !== undefined && seconds <= 86_400) {
+            fiveHour = normalized;
+        }
+        else if (seconds !== undefined && seconds >= 432_000) {
+            weekly = normalized;
+        }
+        else if (rawWindows.length > 1 && index === 0) {
+            fiveHour = normalized;
+        }
+        else {
+            weekly = normalized;
+        }
+    }
+    return { fiveHour, weekly };
 }
 async function resolveCodexAuthHeaders(ctx) {
     const models = codexAuthCandidateModels(ctx);
--- a/dist/index.js
+++ b/dist/index.js
@@ -89,26 +89,20 @@
     }
     return result;
 }
+function renderQuotaWindow(label, quota, theme) {
+    const pct = Math.round(quota.percentage * 10) / 10;
+    const base = `${theme.fg("muted", label)} ${colorForPercentage(pct, theme)(`${pct}%`)}`;
+    const reset = quota.timeRemaining;
+    return reset && reset !== "0" ? `${base} ${theme.fg("dim", `(${reset})`)}` : base;
+}
 /** Render Z.ai usage data into a themed string showing 5h, weekly, and monthly quotas */
 export function renderZaiStatus(data, theme) {
-    const parts = [];
-    // 5-hour quota
-    {
-        const pct = Math.round(data.fiveHour.percentage * 10) / 10;
-        const reset = data.fiveHour.timeRemaining ?? "";
-        parts.push(`${theme.fg("muted", "5h")} ${colorForPercentage(pct, theme)(`${pct}%`)} ${theme.fg("dim", `(${reset})`)}`);
-    }
-    // Weekly quota
-    {
-        const pct = Math.round(data.weekly.percentage * 10) / 10;
-        const reset = data.weekly.timeRemaining ?? "";
-        parts.push(`${theme.fg("muted", "W")} ${colorForPercentage(pct, theme)(`${pct}%`)} ${theme.fg("dim", `(${reset})`)}`);
-    }
-    // Monthly quota
+    const parts = [
+        renderQuotaWindow("5h", data.fiveHour, theme),
+        renderQuotaWindow("W", data.weekly, theme),
+    ];
     if (data.monthly) {
-        const pct = Math.round(data.monthly.percentage * 10) / 10;
-        const reset = data.monthly.timeRemaining ?? "";
-        parts.push(`${theme.fg("muted", "M")} ${colorForPercentage(pct, theme)(`${pct}%`)} ${theme.fg("dim", `(${reset})`)}`);
+        parts.push(renderQuotaWindow("M", data.monthly, theme));
     }
     return `${theme.fg("muted", "Z.ai:")} ${parts.join(` ${theme.fg("dim", "·")} `)}`;
 }
@@ -140,18 +134,14 @@
         : currencies.map((currency) => theme.fg("error", formatMoney(0, currency)));
     return `${theme.fg("muted", "DeepSeek:")} ${renderedBalances.join(" ")}`;
 }
-/** Render OpenAI Codex usage data using the same 5h/week shape as Z.ai. */
+/** Render OpenAI Codex usage data using whichever quota windows the API provides. */
 export function renderCodexStatus(data, theme) {
     const parts = [];
-    {
-        const pct = Math.round(data.fiveHour.percentage * 10) / 10;
-        const reset = data.fiveHour.timeRemaining ?? "";
-        parts.push(`${theme.fg("muted", "5h")} ${colorForPercentage(pct, theme)(`${pct}%`)} ${theme.fg("dim", `(${reset})`)}`);
-    }
-    {
-        const pct = Math.round(data.weekly.percentage * 10) / 10;
-        const reset = data.weekly.timeRemaining ?? "";
-        parts.push(`${theme.fg("muted", "W")} ${colorForPercentage(pct, theme)(`${pct}%`)} ${theme.fg("dim", `(${reset})`)}`);
+    if (data.fiveHour) {
+        parts.push(renderQuotaWindow("5h", data.fiveHour, theme));
+    }
+    if (data.weekly) {
+        parts.push(renderQuotaWindow("W", data.weekly, theme));
     }
     return `${theme.fg("muted", "Codex:")} ${parts.join(` ${theme.fg("dim", "·")} `)}`;
 }
--- a/dist/codex-api.d.ts
+++ b/dist/codex-api.d.ts
@@ -4,8 +4,8 @@
  * Uses Pi's openai-codex provider auth and the ChatGPT Codex usage endpoint.
  */
 export interface CodexUsageData {
-    fiveHour: CodexQuotaInfo;
-    weekly: CodexQuotaInfo;
+    fiveHour?: CodexQuotaInfo;
+    weekly?: CodexQuotaInfo;
 }
 export interface CodexQuotaInfo {
     percentage: number;
